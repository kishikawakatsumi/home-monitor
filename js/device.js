"use strict";

export class Device {
  constructor(id, type, name, structure, traits) {
    this.id = id;
    this.type = type;
    this.name = name;
    this.structure = structure;
    this.traits = traits;
  }
}
